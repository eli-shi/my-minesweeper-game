export function buildInitialBoard(): never {
    throw new Error('buildInitialBoard has been removed. The game logic moved to the backend; use server-driven APIs instead.');
}

export function placeBombs(): never {
    throw new Error('placeBombs has been removed.');
}

export function numberBoard(): never {
    throw new Error('numberBoard has been removed.');
}

export function buildEmptyBoard(): never {
    throw new Error('buildEmptyBoard has been removed.');
}